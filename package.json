{
  "name": "klog-js",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf ./dist && tsc && cp ./src/grammar.ohm ./dist/ && pnpm generate",
    "generate": "ohm generateBundles --esm --withTypes 'dist/*.ohm'",
    "generate:dev": "ohm generateBundles --esm --withTypes 'src/*.ohm'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ohm-js": "^17.1.0"
  },
  "devDependencies": {
    "@ohm-js/cli": "^2.0.0",
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5",
    "vitest": "^1.5.3"
  }
}
